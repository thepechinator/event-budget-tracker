<%= form_for(@spending_pool) do |f| %>
  <% if @spending_pool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spending_pool.errors.count, "error") %> prohibited this spending_pool from being saved:</h2>

      <ul>
      <% @spending_pool.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source %><br>
    <%= f.text_field :source %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :received_on %><br>
    <%= f.datetime_select :received_on %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.text_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
